#include<bits/stdc++.h>
using namespace std;
int const MAXN = 1e5+9;
int vis[MAXN];
unordered_map<string,set<int>>mp;
int main(){
    int n;
    cin  >> n;
    string s;
    cin >> s;
    for (int  len = 0;
    .0 < n/2; len++){
        for (int j = i; j < (n-i)/2; j++){
            int len = i
        }
    }
    cout << ans << endl;
}
